@model Student_manage.Models.Admins

@{
                /**/

                ViewBag.Title = "AdminLogin";
                Layout = null; // Optional: disables _Layout.cshtml for standalone view
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Admin Login</title>
    <!-- ✅ jQuery first -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <!-- ✅ Bootstrap JS (optional, for UI) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/jquery-validation@1.19.5/dist/jquery.validate.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/jquery-validation-unobtrusive@3.2.12/dist/jquery.validate.unobtrusive.min.js"></script>


    <!-- ✅ Your custom AJAX script -->
    @*<script>
        $(document).ready(function () {
            $("#adminLoginForm").submit(function (e) {
                e.preventDefault();
                var form = $(this);
                var token = $('input[name="__RequestVerificationToken"]').val();


                 $("#switchToUserLogin").click(function (e) {
                e.preventDefault();
                window.location.href = '@Url.Action("UserLogin", "Account")';
                });

                $.ajax({
                    url: '@Url.Action("AdminLogin", "Shared")',
                    type: 'POST',
                    data: form.serialize(),
                    headers: {
                        'RequestVerificationToken': token
                    },
                    success: function (result) {
                        if (result.success) {
                            window.location.href = result.redirectUrl;
                        } else {
                            $("#error-message").removeClass("d-none").text(result.message);
                        }
                    },
                    error: function () {
                        $("#error-message").removeClass("d-none").text("An unexpected error occurred.");
                    }
                });
            });
        });
        </script>*@


    <style>
        body {
            font-family: Arial;
            background-color: #f5f5f5;
        }

        .login-box {
            background: #fff;
            width: 350px;
            margin: 100px auto;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0,0,0,1);
        }

        .form-control {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

       
        .field-validation-error {
            color: red !important;
            font-size: 0.9rem;
        }
    


    .btn {
    width: 100%;
    padding: 10px;
    background-color: #d9534f;
    color: white;
    border: none;
    border-radius: 4px;
    }

    .error {
    color: red;
    }
    </style>
</head>
<body>
    <div class="login-box">
        <h3 class="text-center mb-4">Admin Login</h3>

    @using (Html.BeginForm())
       {

            @Html.AntiForgeryToken()

        <div class="form-group">
            @Html.LabelFor(m => m.Username)
            @Html.TextBoxFor(m => m.Username, new { @class = "form-control", placeholder = "Enter username" })
            @Html.ValidationMessageFor(m => m.Username, "", new { @class = "field-validation-error text-danger" })
        </div>

        <div class="form-group">
            @Html.LabelFor(m => m.Password)
            @Html.PasswordFor(m => m.Password, new { @class = "form-control", placeholder = "Enter password" })
            @Html.ValidationMessageFor(m => m.Password, "", new { @class = "field-validation-error text-danger" })
        </div>

        if (ViewBag.Message != null)
        {
            <p class="error">@ViewBag.Message</p>
        }

        <button type="submit" id="adminLoginForm" class="btn">Login</button>


        <div>
            <p><a href="@Url.Action("UserLogin", "Account")" id="switchToUserLogin">Switch to User Login</a></p>



            <p><a href="/Account/Adminforgetpass">Forget Password</a></p>

        </div>
     }


    </div>

</body>
</html>
